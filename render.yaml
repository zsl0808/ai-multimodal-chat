services:
  - type: web
    name: ai-conversation
    runtime: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/conversation-1.0.0.jar
    envVars:
      - key: GEMINI_API_KEY
        sync: false
